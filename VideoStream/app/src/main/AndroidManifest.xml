<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.group35.videostream">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PaymentDetailsActivity" />
        <activity android:name=".BroadcastListingActivity" />
        <activity android:name=".Layout" />
        <activity android:name=".TestMainActivity" />
        <activity android:name=".TestMainActivity3" />
        <activity android:name=".TestMainActivity2"></activity>
    </application>

</manifest>
